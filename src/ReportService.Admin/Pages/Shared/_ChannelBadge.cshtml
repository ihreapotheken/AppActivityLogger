@*
    Renders the ingestion-channel pill ("multipart (zip)" / "JSON") for a single report row.
*@
@model ReportService.Admin.ViewModels.RSAReportRowVM
<span class="badge badge-channel-@Model.Channel">@Model.ChannelLabel</span>
